Ball [] bobo;
boolean reacc;

void setup(){
  size(600,600);
  reacc = false;
  bobo = new Ball [10];
  for ( int i = 0 ; i < balls.length; i++ ) {
     balls[i] = new Ball(); 
  }
  //additional code not shown
}

void draw(){
    background(0);
    for ( int i = 0; i < balls.length )
}